I"ê<p>This snippet was extracted from <a href="https://gist.github.com/sekati/3172554">https://gist.github.com/sekati/3172554</a></p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="c"># xcode-build-bump.sh</span>
<span class="c"># @desc Auto-increment the build number every time the project is run. </span>
<span class="c"># @usage</span>
<span class="c"># 1. Select: your Target in Xcode</span>
<span class="c"># 2. Select: Build Phases Tab</span>
<span class="c"># 3. Select: Add Build Phase -&gt; Add Run Script</span>
<span class="c"># 4. Paste code below in to new "Run Script" section</span>
<span class="c"># 5. Drag the "Run Script" below "Link Binaries With Libraries"</span>
<span class="c"># 6. Insure that your starting build number is set to a whole integer and not a float (e.g. 1, not 1.0)</span>
 
<span class="nv">buildNumber</span><span class="o">=</span><span class="si">$(</span>/usr/libexec/PlistBuddy <span class="nt">-c</span> <span class="s2">"Print CFBundleVersion"</span> <span class="s2">"</span><span class="k">${</span><span class="nv">PROJECT_DIR</span><span class="k">}</span><span class="s2">/</span><span class="k">${</span><span class="nv">INFOPLIST_FILE</span><span class="k">}</span><span class="s2">"</span><span class="si">)</span>
<span class="nv">buildNumber</span><span class="o">=</span><span class="k">$((</span><span class="nv">$buildNumber</span> <span class="o">+</span> <span class="m">1</span><span class="k">))</span>
/usr/libexec/PlistBuddy <span class="nt">-c</span> <span class="s2">"Set :CFBundleVersion </span><span class="nv">$buildNumber</span><span class="s2">"</span> <span class="s2">"</span><span class="k">${</span><span class="nv">PROJECT_DIR</span><span class="k">}</span><span class="s2">/</span><span class="k">${</span><span class="nv">INFOPLIST_FILE</span><span class="k">}</span><span class="s2">"</span></code></pre></figure>
:ET